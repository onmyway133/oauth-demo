{
  "name": "oauth-pkce-browser-app",
  "version": "1.0.0",
  "description": "OAuth 2.0 PKCE flow demo — all OAuth logic runs in the browser",
  "scripts": {
    "start": "bun run src/server.ts",
    "dev": "bun --watch run src/server.ts"
  }
}
